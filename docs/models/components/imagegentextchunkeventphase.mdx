---
title: "ImageGenTextChunkEventPhase"
---

The generation phase this chunk belongs to. `content` is the renderable output; `reasoning` and `draft` are intermediate provider phases.

## Example Usage

```go
import (
	"github.com/OpenRouterTeam/go-sdk/models/components"
)

value := components.ImageGenTextChunkEventPhaseContent

// Open enum: custom values can be created with a direct type cast
custom := components.ImageGenTextChunkEventPhase("custom_value")
```


## Values

| Name                                   | Value                                  |
| -------------------------------------- | -------------------------------------- |
| `ImageGenTextChunkEventPhaseContent`   | content                                |
| `ImageGenTextChunkEventPhaseReasoning` | reasoning                              |
| `ImageGenTextChunkEventPhaseDraft`     | draft                                  |