---
title: "ObservabilityFilterRuleGroupValue"
---

## Supported Types

### 

```go
observabilityFilterRuleGroupValue := components.CreateObservabilityFilterRuleGroupValueStr(string{/* values here */})
```

### 

```go
observabilityFilterRuleGroupValue := components.CreateObservabilityFilterRuleGroupValueNumber(float64{/* values here */})
```

## Union Discrimination

Use the `Type` field to determine which variant is active, then access the corresponding field:

```go
switch observabilityFilterRuleGroupValue.Type {
	case components.ObservabilityFilterRuleGroupValueTypeStr:
		// observabilityFilterRuleGroupValue.Str is populated
	case components.ObservabilityFilterRuleGroupValueTypeNumber:
		// observabilityFilterRuleGroupValue.Number is populated
}
```
