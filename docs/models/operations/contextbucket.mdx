---
title: "ContextBucket"
---

Restrict to requests whose context length falls in this bucket (`1K`, `10K`, `100K`, `1M`, or `10M`). Exact dataset — cannot be combined with `category` or `language_type`.

## Example Usage

```go
import (
	"github.com/OpenRouterTeam/go-sdk/models/operations"
)

value := operations.ContextBucketOneK

// Open enum: custom values can be created with a direct type cast
custom := operations.ContextBucket("custom_value")
```


## Values

| Name                       | Value                      |
| -------------------------- | -------------------------- |
| `ContextBucketOneK`        | 1K                         |
| `ContextBucketTenK`        | 10K                        |
| `ContextBucketOneHundredK` | 100K                       |
| `ContextBucketOneM`        | 1M                         |
| `ContextBucketTenM`        | 10M                        |